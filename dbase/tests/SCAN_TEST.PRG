USE NAMES
? "--- SCAN ALL ---"
SCAN
    ? RECNO(), NAME
ENDSCAN

? "--- SCAN NEXT 2 ---"
GO 1
SCAN NEXT 2
    ? RECNO(), NAME
ENDSCAN

? "--- SCAN FOR ---"
SCAN FOR AGE > 30
    ? RECNO(), NAME, AGE
ENDSCAN

? "--- SCAN WHILE ---"
GO 1
SCAN WHILE AGE < 40
    ? RECNO(), NAME, AGE
ENDSCAN

CLOSE DATABASES
QUIT
